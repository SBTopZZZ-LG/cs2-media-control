[tool.ruff]
target-version = "py311"

[tool.ruff.lint]
# E4/E7/E9 + F: real bugs and style breaks. E501 deliberately off:
# ruff-format already wraps code to 88 cols; E501 would only nag about
# strings/comments/docstrings it cannot split anyway.
select = ["E4", "E7", "E9", "F", "I"]
